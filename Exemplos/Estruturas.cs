using System;

namespace Fundamentos.Exemplos
{
    class Estruturas
    {
        static void Declaracoes()
        {
            int numA;
            int numB = 2, numC = 3;
            const int numD = 4;
            numA = 1;

            Console.WriteLine(numA + numB + numC + numD);
        }
    }
}
